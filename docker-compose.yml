version: '3.0'
services:
  web:
    image: ishtiaque/robishop:1.2
    ports:
      - "80:80"
    links:
      - db
    env_file:
      - env
  db:
    image: mysql:5.6.23
    volumes:
      - './data/var/lib/mysql:/var/lib/mysql:rw'
    env_file:
      - env
